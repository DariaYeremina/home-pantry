import React from 'react';
import styles from './product.module.scss';

const Product = ({title, chosenCategory, chosenUnit, amount}) => (
    <div className={styles.wrapper}>
        
    </div>
)

export default Product;